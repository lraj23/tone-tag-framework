{
	"name": "tone-tag-framework",
	"version": "0.1.0",
	"description": "Lets you write a message with a tone tag and guides those who accidentally run tone tags as commands.",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"start": "node --env-file=.env app.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"license": "MIT",
	"dependencies": {
		"@slack/bolt": "^4.2.0"
	}
}